import { CaretPositionDirective } from './caret-position.directive';

describe('CaretPositionDirective', () => {
  it('should create an instance', () => {
    const directive = new CaretPositionDirective();
    expect(directive).toBeTruthy();
  });
});
